\exerciseset{In Exercises}{, sketch a graph of the given function using \autoref{idea:sketch}. Show all work; check your answer with technology.
}{

\exercise{$\ds f(x) = x^3-2x^2+4x+1$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = -x^3+5x^2-3x+2$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = x^3+3x^2+3x+1$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = x^3-x^2-x+1$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = (x-2)\ln (x-2)$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = (x-2)^2\ln (x-2)$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = \frac{x^2-4}{x^2}$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = \frac{x^2-4x+3}{x^2-6x+8}$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = x+\sin x$ on $[0,2\pi]$.
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = \frac{x^2-2x+1}{x^2-6x+8}$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = x\sqrt{x+1}$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = x^2e^x$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = \sin x \cos x$ on $[-\pi,\pi]$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = (x-3)^{2/3} + 2$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = \frac{(x-1)^{2/3}}{x}$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = \sqrt{\frac{x}{x-5}}$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = \sec x-2\cos x$ on $[0,2\pi]$.
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = x\sqrt{2-x^2}$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = \frac{x}{\sqrt{x^2-1}}$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = x^{5/3}-5x^{2/3}$
}{Use technology to verify sketch.
}

\exercise{$\ds f(x) = \frac{\sin x}{2+\cos x}$ on $[0,2\pi]$.
}{Use technology to verify sketch.
}

}