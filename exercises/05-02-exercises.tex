\printconcepts

\exercise{What is ``total signed area''?}{Answers will vary.}

\exercise{What is ``displacement''?}{Answers will vary.}

\exercise{What is $\ds \int_3^3 \sin x\dd x$?}{0}

\exercise{Give a single definite integral that has the same value as\\
$\ds \int_0^1(2x+3)\dd x + \int_1^2 (2x+3)\dd x$.}{$\int 0^2 (2x+3)\dd x$}

\printproblems

\input{exercises/05-02-exset-01}

\input{exercises/05-02-exset-02}

\input{exercises/05-02-exset-03}

\exercise{An object is thrown straight up with a velocity, in ft/s, given by $v(t) = -32t+64$, where $t$ is in seconds, from a height of 48 feet.
\begin{enumext}
\item		What is the object's maximum velocity?
\item		What is the object's maximum displacement?
\item		When does the maximum displacement occur?
\item		When will the object reach a height of 0? (Hint: find when the displacement is $-48$ft.)
\end{enumext}}{\mbox{}\\[-2\baselineskip]\parbox[t]{\linewidth}{\begin{enumext}
\item		64ft/s
\item		64ft
\item		$t=2$
\item		$t=2+\sqrt{7}\approx 4.65$ seconds
\end{enumext}}}

\exercise{An object is thrown straight up with a velocity, in ft/s, given by $v(t) = -32t+96$, where $t$ is in seconds, from a height of 64 feet.
\begin{enumext}
\item		What is the object's initial velocity?
\item		When is the object's displacement 0?
\item		How long does it take for the object to return to its initial height?
\item		When will the object reach a height of 210 feet?
\end{enumext}}{\mbox{}\\[-2\baselineskip]\parbox[t]{\linewidth}{\begin{enumext}
\item		96ft/s
\item		6 seconds
\item		6 seconds
\item		Never; the maximum height is 208ft.
\end{enumext}}}

\input{exercises/05-02-exset-04}

\input{exercises/05-02-exset-05}

\exercise{Show that for any real $m$ and $b>0$ we have
\[\int_0^b mx\dd x=\frac12 mb^2.\]}{This is a triangle with base $b$ and height $mb$.}

\exercise{Look at signed areas to show that for any continuous function $f$ and any $a$ and $b$ where $a<b$, we have that $\ds\abs{\int_a^b f(x)\dd x}\le\int_a^b\abs{f(x)}\dd x$.}{}

\printreview

\input{exercises/05-02-exset-06}
