\printproblems

\exerciseset{For Exercises}{, calculate $\int_C f(x,y)\,ds$ for the given function $f(x,y)$ and curve $C$.}{

\exercise{$f(x,y)=xy$; $\quad C: x=\cos t$, $y=\sin t$, $0 \le t \le \pi/2$}{$1/2$}

\exercise{$f(x,y)=\dfrac{x}{x^2 + 1}$; $\quad C: x=t$, $y=0$, $0 \le t \le 1$}{}

\exercise{$f(x,y)=2x+y$; $\quad C$: polygonal path from $(0,0)$ to $(3,0)$ to $(3,2)$}{$23$}

\exercise{$f(x,y)=x+y^2$; $\quad C$: path from $(2,0)$ counterclockwise along the circle $x^2 + y^2 = 4$ to the point $(-2,0)$ and then back to $(2,0)$ along the $x$-axis}{}

}

\exercise{Use a line integral to find the lateral surface area of the part of the cylinder $x^2 + y^2 = 4$ below the plane $x+2y+z=6$ and above the $xy$-plane.}{$24\pi$}

\exerciseset{For Exercises}{, calculate $\int_C \dotp{f}{dr}$ for the given vector field $\textbf{f}(x,y)$ and curve $C$.}{

\exercise{$\textbf{f}(x,y) = \textbf{i} - \textbf{j}$; $\quad C: x=3t$, $y=2t$, $0 \le t \le 1$}{}

\exercise{$\textbf{f}(x,y) = y\,\textbf{i} - x\,\textbf{j}$; $\quad C: x=\cos t$, $y=\sin t$, $0 \le t \le 2\pi$}{$-2\pi$}

\exercise{$\textbf{f}(x,y) = x\,\textbf{i} + y\,\textbf{j}$; $\quad C: x=\cos t$, $ y=\sin t$, $0 \le t \le 2\pi$}{}

\exercise{$\textbf{f}(x,y) = (x^2 - y)\,\textbf{i} + (x-y^2 )\,\textbf{j}$; $\quad C: x=\cos t$, $y=\sin t$, $0 \le t \le 2\pi$}{$2\pi$}

\exercise{$\textbf{f}(x,y) = xy^2 \,\textbf{i} + xy^3 \,\textbf{j}$; $\quad C:$ the polygonal path from $(0,0)$ to $(1,0)$ to $(0,1)$ to $(0,0)$}{}

\exercise{$\textbf{f}(x,y) =(x^2 + y^2 )\,\textbf{i}$; $\quad C: x=2+\cos t$, $y=\sin t$, $0 \le t \le 2\pi$}{$0$}

}

\exercise{Verify that the value of the line integral in \autoref{exmp_lineintcyl} is unchanged when using the parametrization of the circle $C$ given in Equation \eqref{eqn:lineintcylcwise}.}{}

\exercise{Show that if $\textbf{f} \perp \textbf{r}\primeskip'(t)$ at each point $\textbf{r}(t)$ along a smooth curve $C$, then $\int_C \dotp{f}{dr} = 0$.}{}

\exercise{Show that if $\textbf{f}$ points in the same direction as $\textbf{r}\primeskip'(t)$ at each point $\textbf{r}(t)$ along a smooth curve $C$, then $\int_C \dotp{f}{dr} = \int_C \norm{\textbf{f}}\,ds$.}{}

\exercise{Prove that $\int_C f(x,y)\,ds = \int_{-C} f(x,y)\,ds$. (\emph{Hint: Use Equations \eqref{eqn:reversec}.})}{}

\exercise{Let $C$ be a smooth curve with arc length $L$, and suppose that $\textbf{f}(x,y) = P(x,y)\,\textbf{i} + Q(x,y)\,\textbf{j}$ is a vector field such that $\norm{\textbf{f}(x,y)} \le M$ for all $(x,y)$ on $C$. Show that\\$\abs{\int_C \dotp{f}{dr}} \le ML$. (\emph{Hint: Recall that $\abs{\int_a^b g(x)\,dx} \le \int_a^b \abs{g(x)}\,dx$ for Riemann integrals.})}{}

\exercise{Prove that the Riemann integral $\int_a^b f(x)\,dx$ is a special case of a line integral.}{}
