\printproblems

\exerciseset{In Exercises}{, calculate $\int_C f(x,y,z)\,ds$ for the given function $f(x,y,z)$ and curve $C$.}{

\exercise{$f(x,y,z)=z$; $\quad C: x=\cos t$, $y=\sin t$, $z=t$, $0 \le t \le 2\pi$}{$2\sqrt2\pi^2$}

\exercise{$f(x,y,z)=\dfrac{x}{y} + y + 2yz$; $\quad C: x=t^2$, $y=t$, $z=1$, $1 \le t \le 2$}{$(17\sqrt{17}−5\sqrt5)/3$}

\exercise{$f(x,y,z)=z^2$; $\quad C: x=t\sin t$, $y=t\cos t$, $z=\frac{2\sqrt{2}}{3}t^{3/2}$, $0 \le t \le 1$}{$2/5$}

}

\exerciseset{In Exercises}{, calculate $\int_C \Dotprod{\textbf{f}}{d\textbf{r}}$ for the given vector field $\textbf{f}(x,y,z)$ and curve $C$.}{

\exercise{$\textbf{f}(x,y,z) = \textbf{i} - \textbf{j} + \textbf{k}$; $\quad C: x=3t$, $y=2t$, $z=t$, $0 \le t \le 1$}{2}

\exercise{$\textbf{f}(x,y,z) = y\,\textbf{i} - x\,\textbf{j} + z\,\textbf{k}$; $\quad C: x=\cos t$, $y=\sin t$, $z=t$, $0 \le t \le 2\pi$}{$2\pi(\pi-1)$}

\exercise{$\textbf{f}(x,y,z) = x\,\textbf{i} + y\,\textbf{j} + z\,\textbf{k}$; $\quad C: x=\cos t$, $y=\sin t$, $z=2$, $0 \le t \le 2\pi$}{}

\exercise{$\textbf{f}(x,y,z) = (y-2z)\,\textbf{i} + xy\,\textbf{j} + (2xz+y)\,\textbf{k}$; $\quad C: x=t$, $y=2t$, $z = t^2 - 1$, $0 \le t \le 1$}{$67/15$}

\exercise{$\textbf{f}(x,y,z) = yz\,\textbf{i} + xz\,\textbf{j} + xy\,\textbf{k}$; $\quad C:$ the polygonal path from $(0,0,0)$ to $(1,0,0)$ to $(1,2,0)$}{}

\exercise{$\textbf{f}(x,y,z) = xy\,\textbf{i} + (z-x)\,\textbf{j} + 2yz\,\textbf{k}$; $\quad C:$ the polygonal path from $(0,0,0)$ to $(1,0,0)$ to $(1,2,0)$ to $(1,2,-2)$}{6}

}

\exerciseset{In Exercises}{, state whether or not the vector field $\textbf{f}(x,y,z)$ has a potential in $\mathbb{R}^{3}$ (you do not need to find the potential itself).}{

\exercise{$\textbf{f}(x,y,z) = y\,\textbf{i} - x\,\textbf{j} + z\,\textbf{k}$}{}

\exercise{$\textbf{f}(x,y,z) = a\,\textbf{i} + b\,\textbf{j} + c\,\textbf{k}$ ($a$, $b$, $c$ constant)}{Yes}

\exercise{$\textbf{f}(x,y,z) = (x+y)\,\textbf{i} + x\,\textbf{j} + z^2 \,\textbf{k}$}{}

\exercise{$\textbf{f}(x,y,z) = xy\,\textbf{i} - (x-yz^2 )\,\textbf{j} + y^2 z\,\textbf{k}$}{No}

}

\exerciseset{In Exercises}{, verify Stokes' Theorem for the given vector field $\textbf{f}(x,y,z)$ and surface $\Sigma$.}{

\exercise{$\textbf{f}(x,y,z) = 2y\,\textbf{i} - x\,\textbf{j} + z\,\textbf{k}$; $\quad \Sigma: x^2 + y^2 + z^2 = 1$, $z \ge 0$}{}

\exercise{$\textbf{f}(x,y,z) = xy\,\textbf{i} + xz\,\textbf{j} + yz\,\textbf{k}$; $\quad \Sigma: z=x^2 + y^2$, $z \le 1$}{}

}

\exercise{Construct a M\"{o}bius strip from a piece of paper, then draw a line down its center (like the dotted line in \autoref{fig:mobius}(b)). Cut the M\"{o}bius strip along that center line completely around the strip. How many surfaces does this result in? How would you describe them? Are they orientable?}{}

\exercise{Use a computer algebra system to plot the M\"{o}bius strip parametrized as:
  \[
   \textbf{r}(u,v) = \cos u \,(1+v\cos \tfrac{u}{2})\,\textbf{i} + \sin u \,(1+v\cos \tfrac{u}{2})\,\textbf{j} +
    v\sin \tfrac{u}{2}\,\textbf{k} , 0 \le u \le 2\pi , -\tfrac{1}{2} \le v \le \tfrac{1}{2}
  \]}{}

\exercise{Let $\Sigma$ be a closed surface and $\textbf{f}(x,y,z)$ a smooth vector field. Show that $\ds\iint_{\Sigma} \Dotprod{(\text{curl }{\textbf{f}}\,)}{\textbf{n}}\,d\sigma = 0$. (\emph{Hint: Split $\Sigma$ in half.})}{}

\exercise{Show that Green's Theorem is a special case of Stokes' Theorem.}{Hint: Think of how a vector field $f(x,y)=P(x,y)i+Q(x,y)j$ in $\mathbb{R}^{2}$ can be extended in a natural way to be a vector field in $\mathbb{R}^{3}$.}
